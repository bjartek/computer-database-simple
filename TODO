Tasks that needs to be done to port to mongodb
 x add import in build configuration
 X remove database configuration
 X add configuration of mongodb and ability to import data into mongodb
 X routes, mongodb uses strings for id's
 X views, mongodb uses strings for id's and DAO pattern not ActiveRecord
 X model, change case classes to use Casbah type id's and implement DAO's
 X controllers, change methods to use DAO's
