Tasks that needs to be done to port to mongodb
 X enable salat plugin in Build configuration
 X remove database configuration and add mongodb configuration
 - routes, mongodb uses ObjectId for id's
 - views, mongodb uses ObjectId for id's 
 X model, change case classes to use Casbah type id's and implement the needed queries
 - controllers, change methods to use the new ModelCompanion queries. 
